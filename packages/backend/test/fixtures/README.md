# Backend Fixtures

Small sample payloads for hook parser and speakable-filter tests.

- `response-fixtures.json` is valid JSON.
- `malformed-hook-input.txt` is intentionally broken JSON.
- Treat `rawText` as the source reply text. The other shapes just test parser detection.
