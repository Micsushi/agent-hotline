{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core IPC for the main panel and the tray mini popup. Without this, frontend event listeners are denied and the auto-refresh loop never starts.",
  "windows": ["main", "mini"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:default",
    "core:window:allow-show",
    "core:window:allow-hide",
    "core:window:allow-set-focus",
    "core:window:allow-set-position"
  ]
}
